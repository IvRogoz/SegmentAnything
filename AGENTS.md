## Instruction Fidelity

Implement the user's stated design and referenced example directly. Do not replace requested UI, behavior, controls, scope, or workflow with a simplified interpretation unless the user explicitly approves that substitution.
